# Required: Gemini API key used by /api/reports/ai-generate
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Optional: override default Gemini model (default: gemini-2.5-flash)
GEMINI_MODEL="gemini-2.5-flash"

# Optional: server-side timeout for Gemini generation request in milliseconds.
# Default: 45000 (45s), lower than Vercel function timeout to return graceful errors.
GEMINI_REQUEST_TIMEOUT_MS="45000"

# Optional: cap generation output size to reduce latency and timeout risk.
# Default: 3000
GEMINI_MAX_OUTPUT_TOKENS="3000"

# Environment mode for backend behavior:
# - dev: enables development-only helpers (such as proxy-forced Gemini requests)
# - prod: disables development-only helpers
APP_ENV="dev"

# Optional dev proxy settings. Applied only when APP_ENV=dev (or NODE_ENV is not production).
HTTPS_PROXY="http://127.0.0.1:7890"
HTTP_PROXY="http://127.0.0.1:7890"
ALL_PROXY="socks5://127.0.0.1:7890"

# Optional but recommended in Vercel: connect Vercel KV for persistent data.
# If omitted, API uses in-memory fallback (works, but data is not durable).
KV_REST_API_URL=""
KV_REST_API_TOKEN=""
